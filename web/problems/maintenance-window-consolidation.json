{
  "page": "maintenance-window-consolidation",
  "title": "Maintenance Window Consolidation",
  "source": "Merge Intervals",
  "difficulty": "Medium",
  "brief": [
    "Several teams file maintenance windows against the same cluster, and many of them overlap. The on-call dashboard should show the combined periods during which the cluster is under maintenance, with no overlapping entries.",
    "Implement consolidateWindows(intervals), where intervals is a list of [start, end] windows, and return the combined windows as a list of [start, end] pairs."
  ],
  "examples": [
    {
      "input": "intervals = [[5,6],[1,3],[2,4]]",
      "output": "[[1,4],[5,6]]"
    }
  ],
  "starterCode": {
    "python": "class Solution:\n    def consolidateWindows(self, intervals: list[list[int]]) -> list[list[int]]:\n        # Think out loud as you go!\n        pass\n",
    "javascript": "/**\n * @param {number[][]} intervals\n * @return {number[][]}\n */\nfunction consolidateWindows(intervals) {\n  // Think out loud as you go!\n}\n",
    "c": "/**\n * Return an array of arrays of size *returnSize.\n * The sizes of the arrays are returned as *returnColumnSizes array.\n * Note: Both returned array and *columnSizes array must be malloced, assume caller calls free().\n */\nint** consolidateWindows(int** intervals, int intervalsSize, int* intervalsColSize, int* returnSize, int** returnColumnSizes) {\n    // Think out loud as you go!\n    *returnSize = 0;\n    *returnColumnSizes = NULL;\n    return NULL;\n}\n",
    "cpp": "class Solution {\npublic:\n    vector<vector<int>> consolidateWindows(vector<vector<int>>& intervals) {\n        // Think out loud as you go!\n        return {};\n    }\n};\n",
    "java": "class Solution {\n    public int[][] consolidateWindows(int[][] intervals) {\n        // Think out loud as you go!\n        return new int[][]{};\n    }\n}\n"
  },
  "interviewMetadata": {
    "difficulty": "Medium",
    "reactoStages": [
      "repeat",
      "example",
      "algorithm",
      "coding",
      "test",
      "optimizations"
    ],
    "followUpDirections": [
      {
        "stage": "repeat",
        "direction": "Ask the candidate to restate the inputs, outputs, constraints, and ambiguities."
      },
      {
        "stage": "example",
        "direction": "Ask the candidate to choose and trace an ordinary example and a boundary case."
      },
      {
        "stage": "algorithm",
        "direction": "Ask for the candidate's approach, correctness argument, and complexity."
      },
      {
        "stage": "coding",
        "direction": "Ask the candidate to implement their stated approach and explain major decisions."
      },
      {
        "stage": "test",
        "direction": "Ask the candidate to predict useful cases and expected results before running them."
      },
      {
        "stage": "optimizations",
        "direction": "Ask for complexity, an uncovered edge case, and a justified optimization or cleanup."
      }
    ]
  }
}
